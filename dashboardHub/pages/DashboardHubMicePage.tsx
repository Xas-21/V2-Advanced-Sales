import React from 'react';
import DashboardHubComingSoon from '../DashboardHubComingSoon';

export default function DashboardHubMicePage({ colors }: { colors: any }) {
    return <DashboardHubComingSoon tabLabel="MICE" colors={colors} />;
}
