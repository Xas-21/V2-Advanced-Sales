import React from 'react';
import DashboardHubComingSoon from '../DashboardHubComingSoon';

export default function DashboardHubAccountsPage({ colors }: { colors: any }) {
    return <DashboardHubComingSoon tabLabel="Accounts" colors={colors} />;
}
