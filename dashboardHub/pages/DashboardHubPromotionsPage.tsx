import React from 'react';
import DashboardHubComingSoon from '../DashboardHubComingSoon';

export default function DashboardHubPromotionsPage({ colors }: { colors: any }) {
    return <DashboardHubComingSoon tabLabel="Promotions" colors={colors} />;
}
